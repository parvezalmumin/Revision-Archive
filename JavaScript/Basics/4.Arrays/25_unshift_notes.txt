unshift() in JavaScript Arrays:

- unshift() is a mutating array method.
- It is used to add one or more elements to the beginning of an array.
- It changes the original array.
- It returns the new length of the array.