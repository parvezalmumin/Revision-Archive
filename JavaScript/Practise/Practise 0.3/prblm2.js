// Create a method to return larger of the two numbers

/*
function largeNum(){
  let num1 = 10;
  let num2 = 11;
  if(num1 > num2){
    return 'num1 is larger';
  } else {
    return 'num2 is larger';
  }
}

console.log(largeNum())

*/

/*
function larger(num1, num2){
    if(num1 > num2){
    return 'num1 is larger';
  } else {
    return 'num2 is larger';
  }
}

console.log(larger(4,5));
console.log(larger(5,5));

*/

/*

function larger(num1, num2) {
  if (num1 > num2) {
    return 'num1 is larger';
  } else if (num2 > num1) {
    return 'num2 is larger';
  } else {
    return 'Both numbers are equal';
  }
}

console.log(larger(4, 5)); // num2 is larger
console.log(larger(5, 5)); // Both numbers are equal
console.log(larger(8, 3)); // num1 is larger

*/

/*

function larger(num1, num2) {
  if (num1 > num2) {
    return num1;
  } else if (num2 > num1) {
    return num2;
  } else {
    return "Both are equal";
  }
}

console.log(larger(4, 5)); // 5
console.log(larger(8, 3)); // 8
console.log(larger(5, 5)); // Both are equal

*/